# Agent Notes

- For Docker-specific verification, it is acceptable to use a temporary directory on the SSH-accessible server when local sandboxing or platform differences make Docker checks unreliable.
